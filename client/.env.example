# URL of the deployed backend (Render). Used as the API + Socket.io origin.
# Leave EMPTY for local dev — the Vite proxy forwards to localhost:4000.
# In production set this in Vercel (or Render) project settings, e.g.:
VITE_API_URL=https://your-backend.onrender.com
